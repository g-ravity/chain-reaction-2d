type Room {
  id: String
  roomId: String
  memberCount: Int
  maxMemberCount: Int
  isActive: Boolean
  hasGameStarted: Boolean
  roomType: GameRoomEnum
}

input CreateRoomArgs {
  maxMemberCount: Int!
}

enum GameRoomEnum {
  PUBLIC
  PRIVATE
}
